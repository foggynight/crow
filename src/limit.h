// limit.h - value limit macros

#ifndef LIMIT_H
#define LIMIT_H

// Size of the word buffer, including the null, used by the reader when reading
// words. Thus the max word length it can read is (WORD_MAX - 1).
#define WORD_MAX 4096

#endif // LIMIT_H
